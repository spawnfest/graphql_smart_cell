# Demo Notebook

```elixir
Mix.install([
  {:graphql_smart_cell, path: "/Users/jason/Workspace/elixirs/graphql_smart_cell"}
])
```

## Section

```elixir
client = AbsintheClient.attach(Req.new(base_url: "http://localhost:4000/api"))
```
